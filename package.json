{
  "name": "telegram-verification-bot",
  "version": "1.0.0",
  "description": "Telegram bot for member verification and anti-bot protection using Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy --env=\"\"",
    "setup-db": "wrangler d1 execute telegram_verification --file=./schema/schema.sql",
    "build": "node scripts/build.js"
  },
  "keywords": [
    "telegram",
    "bot",
    "verification",
    "cloudflare",
    "workers",
    "d1"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
